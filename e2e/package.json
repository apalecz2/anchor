{
  "name": "anchor-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tier 4 end-to-end tests — drive the real Tauri app via tauri-driver + WebdriverIO (TEST_PLAN §7, §8).",
  "scripts": {
    "e2e": "wdio run wdio.conf.ts",
    "e2e:setup": "wdio run wdio.conf.ts --spec ./specs/setup.e2e.ts"
  },
  "devDependencies": {
    "@wdio/cli": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9",
    "typescript": "~5.8.3",
    "webdriverio": "^9"
  }
}
