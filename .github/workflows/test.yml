name: tests

# Mirrors TEST_PLAN §10. Fast Tiers 1–3 gate every PR; the gated/slow Rust suites
# and Tier 4/5 run on the nightly + pre-release matrix so they never block the fast
# feedback loop.

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 6 * * *' # nightly gated/E2E run

jobs:
  frontend:
    name: Frontend (Tiers 1–2)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run test:cov

  rust:
    name: Rust (Tier 3, non-gated)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    defaults:
      run:
        working-directory: app/src-tauri
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: app/src-tauri
      - run: cargo fmt --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test --lib

  gated-and-e2e:
    name: Gated Rust + E2E (nightly / pre-release)
    if: github.event_name == 'schedule'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      # Gated Rust suites (need the real pdfium archive / a tesseract on PATH).
      - name: Gated cargo tests
        working-directory: app/src-tauri
        env:
          PDFIUM_TGZ: ${{ runner.temp }}/pdfium.tgz
          TESSERACT_TEST: '1'
        run: cargo test --lib
      # Tier 4 E2E: build the app, then drive it with tauri-driver + wdio.
      - name: Install tauri-driver
        run: cargo install tauri-driver --locked
      - name: Build app + run E2E
        run: |
          cd app && npm ci && npm run tauri build
          cd ../e2e && npm ci && npm run e2e
